# 🛡️ Aegis Bot: Система защиты пользователей от мошенников

![Python](https://img.shields.io/badge/Python-3.10%2B-blue?style=for-the-badge&logo=python)
![Security](https://img.shields.io/badge/Security-InfoSec-red?style=for-the-badge&logo=security)
![Aiogram](https://img.shields.io/badge/Aiogram-Async-lightgrey?style=for-the-badge)

**Aegis** (Эгида) — это автоматизированная система безопасности для Telegram-чатов, которая защищает пользователей от мошенников.

## 🎯 Цели проекта

1.  **Безопасность:** Оперативное реагирование на триггер-слова и подозрительную активность.
2.  **Сбор метрик:** Анализ поведения пользователей (User Behavior Analytics) для выявления аномалий.

## ⚙️ Архитектура и Функционал

Система построена по модульному принципу:

### 1. Ядро анализа (`aegis_analyzer_v5.py`)
Центральный модуль, отвечающий за логику фильтрации:
*   **Эвристический анализ:** Проверка сообщений по базе сигнатур и ключевых слов.
*   **Система репутации:** Автоматическое понижение рейтинга пользователя при нарушениях.
*   **Логирование:** Запись всех инцидентов для последующего аудита.

### 2. Управление доступом (`admin_panel.py`)
Интерфейс для владельца бота:
*   Просмотр базы нарушителей (`JSON/SQLite`).
*   Горячая блокировка/разблокировка пользователей.
*   Настройка чувствительности фильтров.

### 3. База данных пользователей
Хранение профилей в формате JSON с историей нарушений и текущим статусом (Active/Banned/Muted).

## 🛠️ Стек технологий

*   **Язык:** Python 3.11
*   **API Wrapper:** `aiogram 3.x` (асинхронная обработка событий).
*   **Data Persistence:** Структурированные JSON-файлы (прототип NoSQL хранилища).
*   **Алгоритмы:** Кастомные фильтры текста и регулярные выражения (RegEx).

## 🚀 Быстрый старт

### Требования
*   Python 3.10 или выше
*   Токен бота от `@BotFather`

### Установка

1.  **Клонируйте репозиторий:**
    ```
    git clone https://github.com/arrisstocrat/aegis.git
    cd aegis
    ```

2.  **Активируйте виртуальное окружение:**
    ```
    python -m venv venv
    # Windows:
    venv\Scripts\activate
    # Linux/Mac:
    source venv/bin/activate
    ```

3.  **Установите зависимости:**
    ```
    pip install -r requirements.txt
    ```

4.  **Настройте переменные окружения:**
    Создайте файл `.env` и добавьте:
    ```
    BOT_TOKEN=ваш_токен
    ADMIN_ID=ваш_id
    ```

5.  **Запустите бота:**
    ```
    python bot_v5.py
    ```

## 🔮 Roadmap (Планы развития)
*   [ ] Интеграция с OpenAI API для анализа контекста сообщений (NLP).
*   [ ] Переход на PostgreSQL для работы с HighLoad нагрузками.
*   [ ] Добавление капчи для новых пользователей (защита от user-ботов).

## 👨‍💻 Автор

**@arrisstocrat**
*   *Проект создан с целью изучения методов защиты информации в мессенджерах.*
*(c) 2025 Aegis Security Project.*
